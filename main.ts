/**
 * did from workspace called "notes: If else conditons" we did the first 2
 */
/**
 * do assigment in the workspace called "If Else: Assignment 3"
 */
input.onButtonPressed(Button.A, function () {
    if (rain == true) {
        basic.showString("Raincoat")
    } else {
        basic.showString("Shorts")
    }
})
let rain = false
rain = true
